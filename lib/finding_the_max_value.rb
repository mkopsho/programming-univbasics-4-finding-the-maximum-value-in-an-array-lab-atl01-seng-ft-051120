# This is specifically against instructions
# def find_max_value(array)
#   array.sort!
#   array[-1]
# end

def find_max_value(array)
  count = 0
  while count < array.length do
    array[count]
  end
end